[package]
name = "geekclaw-agent"
description = "Agent engine, session management, output sinks, and tool execution for GeekClaw"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
# Desktop control (screenshots, synthetic input). Gated because xcap/enigo
# pull native display-server libraries that headless hosts must not require.
computer-use = ["dep:geekclaw-computer"]
# Browser automation (in-process self-hosted CDP engine). Gated because it pulls
# the chromiumoxide CDP stack + a managed/connected Chromium; headless hosts that
# never drive a browser must not require it.
browser-use = ["dep:geekclaw-browser", "dep:base64"]

[dependencies]
geekclaw-types.workspace     = true
geekclaw-protocol.workspace  = true
geekclaw-config.workspace    = true
geekclaw-providers.workspace = true
geekclaw-tools.workspace     = true
geekclaw-mcp.workspace       = true
geekclaw-skills.workspace    = true
geekclaw-memory.workspace    = true
geekclaw-compact.workspace   = true
geekclaw-redact.workspace    = true
geekclaw-process-runtime.workspace = true
nomifun-common.workspace = true
geekclaw-computer = { workspace = true, optional = true }
geekclaw-browser = { workspace = true, optional = true }
# Used only by the browser-use SessionVisualLocator adapter (base64-encode the
# screenshot PNG into a ContentBlock::Image for the vision model). Optional →
# pulled in only with the browser-use feature.
base64 = { workspace = true, optional = true }

tracing.workspace     = true
tokio.workspace       = true
futures.workspace     = true
serde.workspace       = true
serde_json.workspace  = true
schemars               = "1"
async-trait.workspace = true
anyhow.workspace      = true
thiserror.workspace   = true
glob.workspace        = true
chrono.workspace      = true
crossterm.workspace   = true
is-terminal.workspace = true


[dev-dependencies]
geekclaw-mcp = { path = "../nomi-mcp", features = ["test-utils"] }
wiremock.workspace    = true
tokio-test.workspace  = true
tempfile.workspace    = true
mockall.workspace     = true
rstest.workspace      = true
serial_test.workspace = true

# ── Integration test binaries ──────────────────────────────────────────────
[[test]]
name = "e2e"
path = "tests/e2e/mod.rs"
# E2E tests require real API keys; run via: cargo nextest run --profile e2e --test e2e

[[test]]
name = "acceptance"
path = "tests/acceptance/mod.rs"
# Acceptance tests for evolution features; run via: cargo nextest run --profile e2e --test acceptance
