/**
 * @license
 * Copyright 2025-2026 GeekClaw (geekclaw.com)
 * SPDX-License-Identifier: Apache-2.0
 */

import React from 'react';
import ComingSoon from '@renderer/components/ComingSoon';

/**
 * 龙虾盒子 — placeholder entry under the MCP rail. Content is intentionally
 * deferred; the nav entry is wired so the surface exists and stays consistent
 * with the rest of the capability rail.
 */
const LobsterPage: React.FC = () => {
  return <ComingSoon title='龙虾盒子 · 更多能力即将开放' />;
};

export default LobsterPage;
